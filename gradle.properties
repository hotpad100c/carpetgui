# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Mod properties
mod.version=1.0.0
mod.group=ml.mypals.carpetgui
mod.id=carpetgui
mod.name=CarpetGUI

# Global dependencies
deps.fabric_loader=0.18.4
